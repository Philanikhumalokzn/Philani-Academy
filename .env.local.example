# Copy this file to .env.local (DO NOT commit secrets)

# JaaS (8x8) public settings for the frontend
NEXT_PUBLIC_JITSI_DOMAIN=8x8.vc
NEXT_PUBLIC_JITSI_API_URL=https://8x8.vc/<deployment-id>/external_api.js
# Optional fallback room used by the admin dashboard when no session room is active
# NEXT_PUBLIC_JAAS_DASHBOARD_ROOM=vpaas-magic-cookie-.../DashboardRoom

# RS256 signing (recommended): store your private key in a local file
# Do NOT paste the private key into this file. Use PowerShell to load the key
# into the environment at runtime as shown in docs/JaaS_SETUP.md.
JAAS_KEY_ID=<your-kid-here>
JAAS_APP_ID=<your-jaas-app-id>

# Fallback HS256 credentials (if your JaaS account uses API key/secret)
JITSI_JAAS_APP_ID=<your-app-id>
JITSI_JAAS_API_KEY=<your-api-key>
JITSI_JAAS_API_SECRET=<your-api-secret>

# Application secrets
ROOM_SECRET=<your-room-secret>
NEXTAUTH_SECRET=<your-nextauth-secret>
