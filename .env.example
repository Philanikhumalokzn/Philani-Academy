# Copy to .env and fill values
DATABASE_URL="file:./dev.db"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-this-with-a-random-secret"
STRIPE_SECRET="sk_test_..."

# Jitsi configuration
# Public Jitsi domain used by the client. Replace with your self-hosted domain when ready.
NEXT_PUBLIC_JITSI_DOMAIN=meet.example.com
# Optional fallback room used by the admin dashboard when no secure session is active
# NEXT_PUBLIC_JAAS_DASHBOARD_ROOM=vpaas-magic-cookie-.../DashboardRoom

# If you use JWT for Prosody auth, keep the secret server-side only (do NOT expose in NEXT_PUBLIC_)
# Example server-side secret name (use this name in your server env):
JITSI_AUTH_SECRET=super-secret-token
