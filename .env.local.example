# Copy this file to .env.local and fill in the secret values. Do NOT commit .env.local to git.

# NextAuth secret (random string used to sign sessions)
NEXTAUTH_SECRET=change_me_random_secret

# Local app URL
NEXTAUTH_URL=http://localhost:3000

# Stripe keys - paste your secret key (sk_test_... or sk_live_...)
# STRIPE_SECRET_KEY is preferred; STRIPE_SECRET is supported as a fallback.
STRIPE_SECRET_KEY=
STRIPE_SECRET=

# Optional: a fallback price id to use if DB does not have price ids
# STRIPE_PRICE_ID=price_XXXXXXXXXXXX

# Optional publishable key (used on client-side)
# STRIPE_PUBLISHABLE_KEY=pk_test_XXXXXXXXXXXX
